# Static site. Netlify publishes ONLY the public/ directory, which contains nothing but
# index.html. The source data/ CSVs, build_report.py, and README are never uploaded.
# No build step: build_report.py is run locally; only the generated HTML is deployed.
[build]
  publish = "public"
